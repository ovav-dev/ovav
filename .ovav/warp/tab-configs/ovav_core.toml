name = "OVAV CORE"
color = "blue"

[params.worktree]
type = "text"
description = "OVAV worktree path (use ovav worktree owc first)"
default = ""

[[panes]]
id = "main"
type = "terminal"
directory = "{{worktree}}"
shell = "fish"
commands = [
  "ovav status",
  "ovav worktree owl",
]
is_focused = true
