"""OVAV Service Area Router — Agent Runtime L7"""

def route_request(request):
    """Route a request to the appropriate service area."""
    return None

def service_area(agent_id):
    """Return the service area for an agent."""
    return None

def internal_repo_access_denied_by_default(agent_id):
    """Check if repo access is denied by default."""
    return True
