# ovav-verify

Compatibility command.

Prefer `ovav-validate` for current OVAV validation.

Keep this command only as a lightweight fallback for older OpenCode workflows or quick source-local verification.
